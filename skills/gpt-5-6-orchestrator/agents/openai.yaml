interface:
  display_name: "GPT-5.6 Orchestrator"
  short_description: "Sol Max directs dynamic Luna, Terra, and Sol workers."
  default_prompt: "Use $gpt-5-6-orchestrator. Keep GPT-5.6 Sol at max in the main session as planner, subagent controller, consequential decision maker, and final authority; dynamically route bounded work to pinned Luna, Terra, or Sol Codex subagents."
policy:
  allow_implicit_invocation: false
