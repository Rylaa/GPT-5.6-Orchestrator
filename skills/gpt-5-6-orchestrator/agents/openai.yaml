interface:
  display_name: "GPT-5.6 Orchestrator"
  short_description: "Sol directs task-shaped Luna, Terra, and Sol workers."
  default_prompt: "Keep Sol in the main session at the configured reasoning target, clarify material ambiguity, route bounded pinned workers dynamically, and return focused worker test evidence directly to Sol for final acceptance."
policy:
  allow_implicit_invocation: true
