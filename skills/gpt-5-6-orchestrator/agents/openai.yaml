interface:
  display_name: "GPT-5.6 Orchestrator"
  short_description: "Sol Max directs dynamic Luna, Terra, and Sol workers."
  default_prompt: "Keep Sol Max in the main session, clarify material ambiguity, route bounded workers dynamically, and return focused worker test evidence directly to Sol for final acceptance."
policy:
  allow_implicit_invocation: true
