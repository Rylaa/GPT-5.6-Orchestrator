interface:
  display_name: "GPT-5.6 Orchestrator"
  short_description: "Sol Max directs dynamic Luna, Terra, and Sol workers."
  default_prompt: "Keep GPT-5.6 Sol at max in the main session as planner, subagent controller, consequential decision maker, and final authority; decompose the request and dynamically route bounded work to pinned Luna, Terra, or Sol Codex subagents."
policy:
  allow_implicit_invocation: true
